Implement the change described in `rfc.md`, working through Spec Kit.

Take it from a specification to working code: `/speckit.specify`, then
`/speckit.plan`, `/speckit.tasks`, `/speckit.implement`. Write the spec from the
RFC, and keep going until the acceptance criteria in the RFC are met.

The project's design system and where to read it are described in `AGENTS.md`.
Consult it while you specify, plan and implement: reuse what it already offers
before building anything new, and style through its tokens.
